# Web 框架
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6
pydantic-settings>=2.0.0

# 人脸识别核心库
# 升级 numpy 以兼容 Python 3.12 和 opencv-python-headless 4.9+
numpy>=1.26.0,<2.0.0
# 使用较新的 opencv-python-headless
opencv-python-headless==4.9.0.80
# face_recognition 依赖 dlib，通常会自动安装
face-recognition==1.3.0

# 图像处理
Pillow==10.2.0

# 数据库和存储
supabase==2.3.0

# dlib 构建工具 (必须在 dlib 之前安装)
cmake>=3.20.0
setuptools>=68.0.0
wheel>=0.40.0

# dlib (尝试不指定版本，让 pip 选择最佳版本或源码构建)
dlib
